name: NewsMirror summarise (manual-only)

on:
  workflow_dispatch:

permissions:
  issues: write

jobs:
  summarise:
    runs-on: ubuntu-latest
    steps:
      - name: Summarise pending articles (manual trigger)
        id: summarise_call
        run: |
          HTTP_STATUS=$(curl -s -o /tmp/summarise_response.json -w "%{http_code}" -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/ingest-articles?phase=summarise" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            --data '{}')
          echo "HTTP status: $HTTP_STATUS"
          cat /tmp/summarise_response.json
          if [ "$HTTP_STATUS" -lt 200 ] || [ "$HTTP_STATUS" -ge 300 ]; then
            echo "::error::Summarise edge function returned HTTP $HTTP_STATUS"
            exit 1
          fi

      - name: Alert on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const title = `🚨 Summarise workflow failed — ${new Date().toISOString().slice(0,16).replace('T',' ')} UTC`;
            const body = [
              '**Workflow:** NewsMirror summarise (manual)',
              `**Run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}`,
              '',
              'The summarise edge function returned a non-2xx response or the curl call failed.',
              '',
              '**Checklist**',
              '- [ ] Check Supabase Edge Function logs (`ingest-articles?phase=summarise`)',
              '- [ ] Verify Gemini API key is set and quota not exceeded',
              '- [ ] Check `articles` table for unexpected schema changes',
              '- [ ] See `docs/ops-runbook.md` for full triage steps',
            ].join('\n');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body,
              labels: ['ops', 'alert'],
            });
