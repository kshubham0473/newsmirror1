name: NewsMirror summarise

on:
  schedule:
    # Summarise pending articles every 10 mins
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  summarise:
    runs-on: ubuntu-latest
    steps:
      - name: Summarise pending articles
        run: |
          curl -s -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/ingest-articles?phase=summarise" \
            -H "Authorization: Bearer ${{ secrets.eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp0bnZrcmNnemJidXd1amFvemRrIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NDA4NjIxNywiZXhwIjoyMDg5NjYyMjE3fQ.yGgUh2bUQDyy2Arn37AVrJf9WrmDbhePjE3r_-Ckz_U }}" \
            -H "Content-Type: application/json" \
            --data '{}'
